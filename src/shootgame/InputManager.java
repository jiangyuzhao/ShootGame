package shootgame;

/**
 * 这个类是游戏逻辑与设备输入之间的中间层
 * 每次更新游戏状态，游戏物体类在update()方法中读取此类来确认用户是否对某个键有输入
 * 每次侦测到用户输入，则将这个类中的对应标志位更新
 * 请注意将这个类设计成线程安全的
 *
 * @author hehao
 */
public class InputManager {
    // TODO
}
